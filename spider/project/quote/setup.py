# Automatically created by: sparrow
from setuptools import setup, find_packages
setup(
    name='quote',
    version='6.0',
    packages=find_packages(),
    entry_points={'scrapy':['settings=quote.settings']},
)